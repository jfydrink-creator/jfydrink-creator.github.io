{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "command": {
    "build": {
      "description": "Compila el sitio a estático (dist/)",
      "template": "Ejecuta el build del proyecto Astro con npm.cmd run build y verifica que termina sin errores."
    },
    "preview": {
      "description": "Muestra una vista previa local del sitio compilado",
      "template": "Ejecuta npm.cmd run preview y avísame de la URL."
    }
  }
}
